services:
  liuzx-admin:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: liuzx-admin
    image: liuzx-admin:${LIUZX_VERSION}
    env_file:
      - .env
    ports:
      - "9990:9990"
    environment:
      - LANG=zh_CN.UTF-8
      - TZ=Asia/Shanghai
      - JVM_OPTS=-Xms512m -Xmx512m -Xmn256m
    volumes:
      - ./logs:/tmp
      - ./data:/home/admin/data
    init: true
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "3"
    healthcheck:
      test: ["CMD-SHELL", "bash -c ': > /dev/tcp/127.0.0.1/9990'"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    networks:
      pki-network:
        aliases:
          - admin

networks:
  pki-network:
    external: true
    name: pki-network
